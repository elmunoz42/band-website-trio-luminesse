# Standards Applied

See `agent-os/standards/frontend/next-wordpress-theme.md`.

Relevant standards for this spec:

- Keep WordPress integration behind `lib/` adapters.
- Provide local fallback content when WordPress is unavailable.
- Keep the homepage focused on band identity, sound, event fit, and booking.
- Validate lint and production build before deployment-oriented handoff.
