# Standards Applied

See `agent-os/standards/frontend/next-wordpress-theme.md`.

Relevant standards:

- Keep sitewide infrastructure in shared layout-level code.
- Validate `npm run lint` and `npm run build` before commit.
